# VRC 素材整理器 v1.0.2

## 检索改进

- 普通 BOOTH 检索不再固定按最新商品排序，旧的模型本体不会被新上架适配素材挤出结果页。
- 优化文件名拆词、版本号过滤、候选标题/内容物评分与“证据不足则保留候选”的逻辑。
- ZIP 和 Unity Package 会读取包内路径元数据，提取商品目录名作为补充检索线索；首层工作室目录仅用于消歧，不会直接当商品名搜索。
- “模型本体”筛选已识别 `オリジナル3Dモデル`、`3Dキャラクター` 等 BOOTH 常见标题。

## 分级 LLM 检索

- 检索页新增三档：普通检索、智能检索、深度检索。
- 智能检索使用低档文本模型分析文件名、扩展检索词和重排候选。
- 深度检索先做文字判断，只有不确定时才使用高档多模态模型查看最多 4 张 BOOTH 候选封面。
- 新增低档文本模型与高档多模态模型的独立配置；高档地址和密钥可留空以沿用低档服务。
- 普通检索已有强证据时不会调用 LLM；LLM 弃权时也不会清空普通候选。

## 下载

- `VRC素材整理器 Setup 1.0.2.exe`：安装版。
- `VRC素材整理器 1.0.2.exe`：便携版。

请同时下载 `SHA256SUMS-v1.0.2.txt`，使用 PowerShell 的 `Get-FileHash` 验证文件完整性。
