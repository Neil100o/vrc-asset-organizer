# VRC 素材整理器 v1.0.0

首个公开版本。用于本地整理 VRChat / BOOTH 素材包。

## 新功能

- 浏览 ZIP、RAR、7Z 与 Unity Package 素材包，支持缩略图卡片和详情页。
- 在 BOOTH 的 VRChat 分区搜索候选；支持 BOOTH 编号、文件名清理、版本号过滤和手动链接。
- 支持道具、衣服、头发、妆容、功能插件、模型、场景与地图、贴图与材质、ERP 等分类。
- 确认后移动原文件；同名文件会自动避让，避免覆盖。
- 已分类素材、待确认素材、主素材与附属素材关系均可管理。
- 支持从资源管理器拖入素材，并选择移动或复制。
- 可选接入兼容 OpenAI Chat Completions 的 LLM，辅助扩展检索词和筛选候选；不开启也可完整使用普通检索。
- 提供 BOOTH 与 Boothplorer 手动检索入口。

## 下载说明

- `VRC素材整理器 Setup 1.0.0.exe`：安装版，推荐大多数用户使用。
- `VRC素材整理器 1.0.0.exe`：便携版，下载后可直接运行。

Windows 可能会因未签名而显示 SmartScreen 提示；这是个人发布的 Electron 应用尚未购买代码签名证书所致。请只从本仓库的 Releases 下载，并在确认来源后选择继续运行。

## 文件校验

Release 中会附带 `SHA256SUMS-v1.0.0.txt`。下载后可在 PowerShell 中执行以下命令，并将输出与校验文件比对：

```powershell
Get-FileHash '.\VRC素材整理器 Setup 1.0.0.exe' -Algorithm SHA256
```

哈希校验可以确认下载文件没有在传输或镜像过程中被改动，但不能替代杀毒软件或 Windows 代码签名。

## 已知限制

- BOOTH 的页面结构、网络环境和访问限制可能影响自动检索或封面下载。
- 自动匹配只用于提供候选，建议始终在确认页面检查商品与封面。
- 当前仅支持 Windows。
