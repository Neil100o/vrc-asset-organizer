# VRC 素材整理器 v1.0.3

## 搜索可信验证

- 智能检索与深度检索会复核普通搜索的首选候选，不再因普通规则判为强匹配就跳过 LLM。
- LLM 拒绝或无法确定时，不自动绑定商品；候选会保留给用户手动确认。
- 深度检索仍然只在文本判断无法区分时发送候选封面。

## 同族素材整理

- 手动确认一个 BOOTH 商品后，可将当前文件指定为主素材。
- 工具会列出同一英文主名前缀的本地文件，支持逐项勾选。
- 确认后，所选文件继承 BOOTH 链接和封面，并关联为主素材的附属包；不会移动原文件。
- 手动填入并确认的 BOOTH 链接会自动保存为本地参考，以便后续同族包检索。

## 下载

- `VRC素材整理器 Setup 1.0.3.exe`：安装版。
- `VRC素材整理器 1.0.3.exe`：便携版。

请同时下载 `SHA256SUMS-v1.0.3.txt`，使用 PowerShell 的 `Get-FileHash` 验证文件完整性。
